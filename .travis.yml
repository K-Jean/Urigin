language: node_js

notifications:
  webhooks:
    - $SLACK_HOOK

node_js:
  - "node"

script:
  - npm install nyc -g
  - npm install codecov -g
  - npm test

after_success:
  - npm run coverage
  - codecov

services:
  - postgresql
  - docker

deploy:
  provider: script
  script: bash docker_push
  on:
    branch: master